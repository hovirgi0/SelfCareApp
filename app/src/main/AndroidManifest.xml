<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
>


    <application
            android:allowBackup="true"
            android:fullBackupContent="@xml/backup_rules"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.SelfCareApp">

    <!-- Kezdőképernyő -->
        <!--<activity android:name="com.example.selfcareapp.ui.MainActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->

        <!-- Login képernyő -->
        <!-- <activity android:name="com.example.selfcareapp.ui.LoginActivity" /> -->

         <!-- ToDo képernyő -->
        <!--<activity android:name="com.example.selfcareapp.ui.TodoActivity" /> -->

        <!-- Journal képernyő -->
        <!--<activity android:name="com.example.selfcareapp.ui.JournalActivity" /> -->

        <!-- Chat képernyő -->
        <!--<activity android:name="com.example.selfcareapp.ui.ChatActivity" /> -->

        <!-- Kezdőképernyő -->
      <!--  <activity android:name="com.example.selfcareapp.ui.personalization.StartActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->

        <!-- Kezdőképernyő -->
        <activity android:name="com.example.selfcareapp.ui.MainMenuActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

       <activity android:name=".ui.personalization.StartActivity" />
       <activity android:name=".ui.personalization.NameActivity" />
        <activity android:name=".ui.personalization.PreferencesActivity" />
        <!-- <activity android:name=".ui.MainMenuActivity" /> -->



        </application>
    </manifest>